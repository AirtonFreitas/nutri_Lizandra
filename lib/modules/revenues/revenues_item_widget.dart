import 'package:flutter/material.dart';
class RevenuesItem extends StatelessWidget {
  const RevenuesItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('abre aqui a receita'),);
  }
}
