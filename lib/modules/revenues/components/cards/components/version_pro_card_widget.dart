import 'package:flutter/material.dart';

class VersaoPro extends StatelessWidget {
  const VersaoPro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return
      Container(
      child: Text('Programa completo',style: TextStyle(fontSize: 32,fontFamily: 'GeosansLight'),),);
  }
}
