import 'package:flutter/material.dart';

class VersaoFree extends StatelessWidget {
  const VersaoFree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return
      Container(
      child: Text('Receitas Grátis',style: TextStyle(fontSize: 32,fontFamily: 'GeosansLight'),),);
  }
}
